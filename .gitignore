# Downloaded demo binaries / archives — too large for git, host separately
/downloads/
*.zip
*.7z
*.rar
*.exe
*.msi

# Local promo/screenshot assets (third-party press images) — not for the repo
/tweet-assets/

# OS / editor cruft
.DS_Store
Thumbs.db
.claude/
